.venv/
__pycache__/
*.pyc

# results/*.json is deliberately NOT ignored. It is generated, but it is the
# evidence behind the published table -- a reader who cannot see it cannot
# check the numbers.
